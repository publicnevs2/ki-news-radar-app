name: ki_news_radar_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  flutter_riverpod: ^2.5.1
  http: ^1.2.1
  google_fonts: ^6.2.1
  url_launcher: ^6.3.0
  flutter_staggered_animations: ^1.1.1
  share_plus: ^9.0.0
  fl_chart: ^0.68.0
  
  # NEUE PAKETE
  just_audio: ^0.9.39
  audio_service: ^0.18.13
  intl: ^0.19.0 # Für die Datumsformatierung

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/

